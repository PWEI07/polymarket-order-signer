services:
  setup:
    build: .
    command: python setup.py
    volumes:
      - ./.env:/app/.env
    env_file: .env

  signer:
    build: .
    command: uvicorn signer_server:app --host 0.0.0.0 --port 8080
    ports:
      - "8080:8080"
    env_file: .env
    restart: unless-stopped
